{% extends 'base.html.twig' %}

{% block title %}Reinitialiser le mot de passe{% endblock %}

{% block nav %}
{% endblock %}

{% block body %}
<div class="register-page">
    <div class="register-hero">
        <img src="{{ asset('img/site/log.jpeg') }}" alt="DragonTable">
    </div>

    <div class="register-form-container">
        <img src="{{ asset('img/site/logo.png') }}" alt="DragonTable" class="register-logo">

        <h1>Nouveau mot de passe</h1>

        {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword, {
                label: 'Nouveau mot de passe'
            }) }}

            <button class="btn btnsolid btnoutline" type="submit">Reinitialiser</button>
        {{ form_end(resetForm) }}

        <div class="register-links">
            <a href="{{ path('app_login') }}">Retour à la connexion</a>
        </div>
    </div>
</div>
{% endblock %}

